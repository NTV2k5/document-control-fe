import { DocumentInputAgentHistorySection } from '../../sections';

export const DocumentInputAgentHistoryPage = ({ user_id }: { user_id?: string }) => (
  <DocumentInputAgentHistorySection user_id={user_id} />
);
